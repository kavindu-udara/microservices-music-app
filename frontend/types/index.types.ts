export type ArtistType = {
    _id: string,
    name: String,
    image: String,
    description: String,
    createdAt: String,
    updatedAt: String
}

export type SongType = {
    name : string,
    image : string,
    song : string,
    artist: string,
    createdAt: String,
    updatedAt: String
}